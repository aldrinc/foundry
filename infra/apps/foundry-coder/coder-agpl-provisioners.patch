diff --git a/enterprise/coderd/coderd.go b/enterprise/coderd/coderd.go
index a897711..aa2ed3c 100644
--- a/enterprise/coderd/coderd.go
+++ b/enterprise/coderd/coderd.go
@@ -412,7 +412,6 @@ func New(ctx context.Context, options *Options) (_ *API, err error) {
 			r.Use(
 				apiKeyMiddleware,
 				httpmw.ExtractOrganizationParam(api.Database),
-				api.RequireFeatureMW(codersdk.FeatureExternalProvisionerDaemons),
 			)
 			r.Get("/", api.provisionerKeys)
 			r.Post("/", api.postProvisionerKey)
@@ -435,7 +434,6 @@ func New(ctx context.Context, options *Options) (_ *API, err error) {
 		// route as is.
 		r.Route("/organizations/{organization}/provisionerdaemons/serve", func(r chi.Router) {
 			r.Use(
-				api.provisionerDaemonsEnabledMW,
 				apiKeyMiddlewareOptional,
 				httpmw.ExtractProvisionerDaemonAuthenticated(httpmw.ExtractProvisionerAuthConfig{
 					DB:       api.Database,
diff --git a/enterprise/coderd/provisionerdaemons_test.go b/enterprise/coderd/provisionerdaemons_test.go
index 3d9347b..70c9d31 100644
--- a/enterprise/coderd/provisionerdaemons_test.go
+++ b/enterprise/coderd/provisionerdaemons_test.go
@@ -36,6 +36,33 @@ import (
 	"github.com/coder/coder/v2/testutil"
 )
 
+func TestProvisionerDaemonServeWithoutPremiumLicense(t *testing.T) {
+	t.Parallel()
+
+	client, user := coderdenttest.New(t, nil)
+	templateAdminClient, _ := coderdtest.CreateAnotherUser(t, client, user.OrganizationID, rbac.RoleTemplateAdmin())
+	ctx, cancel := context.WithTimeout(context.Background(), testutil.WaitLong)
+	defer cancel()
+
+	daemonName := testutil.MustRandString(t, 63)
+	srv, err := templateAdminClient.ServeProvisionerDaemon(ctx, codersdk.ServeProvisionerDaemonRequest{
+		Name:         daemonName,
+		Organization: user.OrganizationID,
+		Provisioners: []codersdk.ProvisionerType{
+			codersdk.ProvisionerTypeEcho,
+		},
+		Tags: map[string]string{},
+	})
+	require.NoError(t, err)
+	srv.DRPCConn().Close()
+
+	daemons, err := client.ProvisionerDaemons(ctx) //nolint:gocritic // Test assertion.
+	require.NoError(t, err)
+	if assert.Len(t, daemons, 1) {
+		assert.Equal(t, daemonName, daemons[0].Name)
+	}
+}
+
 func TestProvisionerDaemonServe(t *testing.T) {
 	t.Parallel()
 	t.Run("OK", func(t *testing.T) {
diff --git a/enterprise/coderd/provisionerkeys_test.go b/enterprise/coderd/provisionerkeys_test.go
index daca662..fcd6392 100644
--- a/enterprise/coderd/provisionerkeys_test.go
+++ b/enterprise/coderd/provisionerkeys_test.go
@@ -135,6 +135,29 @@ func TestProvisionerKeys(t *testing.T) {
 	require.ErrorContains(t, err, "reserved")
 }
 
+func TestProvisionerKeysWithoutPremiumLicense(t *testing.T) {
+	t.Parallel()
+
+	ctx := testutil.Context(t, testutil.WaitLong)
+	client, owner := coderdenttest.New(t, nil)
+	orgAdmin, _ := coderdtest.CreateAnotherUser(
+		t,
+		client,
+		owner.OrganizationID,
+		rbac.ScopedRoleOrgAdmin(owner.OrganizationID),
+	)
+
+	_, err := orgAdmin.CreateProvisionerKey(ctx, owner.OrganizationID, codersdk.CreateProvisionerKeyRequest{
+		Name: "foundry-no-license",
+		Tags: map[string]string{"scope": "organization", "owner": ""},
+	})
+	require.NoError(t, err)
+
+	keys, err := orgAdmin.ListProvisionerKeys(ctx, owner.OrganizationID)
+	require.NoError(t, err)
+	require.Len(t, keys, 1)
+}
+
 func TestGetProvisionerKey(t *testing.T) {
 	t.Parallel()
 
